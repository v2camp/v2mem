# 构建产物
/bin/
*.db
*.db-wal
*.db-shm

# 临时产物（覆盖率报告等本地可再生视图，统一进 temp/，不入库）
/temp/

# 本地同步导出（跨设备归集用，不入库）
/sync/

# worktree 工作区（一任务一分支一 worktree，见 AGENTS.md「任务流程」）
/.worktree/

# 本地记忆/工具产物（不入库）
/.workbuddy/

# 脑暴可视化草稿（不入库）
/.superpowers/

# 发布评测报告（make release 生成，属本地工件，不入库）
/.mem/

# 编辑器 / 系统
.DS_Store
*.swp
