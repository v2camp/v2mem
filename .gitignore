# 构建产物
/bin/
*.db
*.db-wal
*.db-shm

# 覆盖率产物（报告是本地视图，不入库；门槛以 coverage-policy.txt 为准）
/coverage.out
/coverage.html

# 本地同步导出（跨设备归集用，不入库）
/sync/

# worktree 工作区（一任务一分支一 worktree，见 AGENTS.md「任务流程」）
/.worktree/

# 本地记忆/工具产物（不入库）
/.workbuddy/

# 脑暴可视化草稿（不入库）
/.superpowers/

# 发布评测报告（make release 生成，属本地工件，不入库）
/.mem/

# 编辑器 / 系统
.DS_Store
*.swp
